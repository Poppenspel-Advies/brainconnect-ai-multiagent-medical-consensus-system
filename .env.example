# BrainConnect Frontend Environment Variables
# Copy to .env.local and fill in values

# Backend API URL (adjust if running on different port)
VITE_API_URL=http://localhost:8000

# Fireworks API Key (for direct client-side calls if needed)
# VITE_FIREWORKS_API_KEY=your_key_here